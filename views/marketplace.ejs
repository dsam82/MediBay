<!DOCTYPE html>
<html>
<head>
    <title>MediBay | Profile</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="css/sheet.css">
    <link href='https://fonts.googleapis.com/css?family=Sofia' rel='stylesheet'>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css">
    <!-- Bootstrap core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <!-- Material Design Bootstrap -->
    <link href="css/mdb.min.css" rel="stylesheet">
    <!-- Your custom styles (optional) -->
    <link href="css/style.css" rel="stylesheet">
</head>

<body>
    <% include partials/header %>
    <div class="container">
    <% if (locals.user) { %>
        <div class="col mb-3">
            <p id="market-head">
                Click to open and Request the medicine.
            </p>
            <% for(var i = 0; i< medicine.length; i++) { %>
                <hr>
                <p><span id="photo"><image href="medicine[i].photo"></image></span></p>
                <p><span id="name">{%= medicine[i].name %}</span></p>
                <p><span id="sp">{%= medicine[i].sp %}</span>
                    <span id="mrp"><strike>{%= medicine[i].mrp %}</strike></span></p>
                <p><span id="quantity">{%= medicine[i].quantity %}</span></p>
                <hr>
            <% } %>
        </div>
    <% } else { %>
        <p>
            You are not a valid user. Please <a href="/register">Sign Up</a>
        </p>
        <p>Have an Account?
            <a href="/login">Sign In</a>
        </p>
    <% } %>
</div>
    <% include partials/footer %>
</body>
</html>